// The marketplace's nominal fee on every trip. Cash-only today: the driver
// collects the fare and owes this share back as a settlement. When a payment
// gateway is added, the same rate becomes an automatic deduction.
export const COMMISSION_RATE = 0.1;
